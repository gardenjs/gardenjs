# Tests

Unit tests for `resolveA11ySettings` and `a11y` config helpers (`garden.config.js` → `a11y` merge logic).

```bash
npm test
```

Manual checks for the Accessibility panel use the **Axetest** component in [gardenjs_docs](https://github.com/gardenjs/docs) (`/components/axetest/axetest`).
